---
description: OpenAPI-first DDD codegen — core-only after YAML edits; full scaffold for new domains
globs:
  - packages/openapi-core/**/*
  - packages/core/**/*
  - platform/**/*
  - .codegen/**/*
alwaysApply: false
---

# DDD codegen (Formulary)

- **New domain:** full multi-layer generate once, then hand-fit platform.
- **Existing domain YAML edit:** bundle → regenerate **`packages/core` only** → handwrite services/adapters/api-server/webapp.
- Package scope: `@formulary/*`. Config: `.codegen/.zero-codegen-merged.json` (run `pnpm codegen:paths`).
- Domains: `identity`, `candidates`, `readiness`, `gates`, `investment`, `registry`, `benefits`, `portfolio`.
- Skills: `ddd-platform`, `ddd-codegen`, `ddd-identity`, `ddd-formulary` under `.cursor/skills/`.
- Do not regenerate adapters/services casually — sandbox + nested DI are hand-maintained after scaffold.
- Product specs: root `PRODUCT.md`, `WEBAPP.md`, `USER_STORIES.md`. OpenAPI source of truth: `packages/openapi-core/src/`.
