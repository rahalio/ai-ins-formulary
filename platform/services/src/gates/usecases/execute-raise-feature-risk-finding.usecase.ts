/**
 * ExecuteRaiseFeatureRiskFinding - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: gates
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteRaiseFeatureRiskFinding Use Case
 *
 * DDD: Application use case for get FeatureRiskFinding.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { RaiseFeatureRiskFindingInput, RaiseFeatureRiskFindingOutput } from "../dto/feature-risk-finding.dto";
import type { ExecutionContextService, IdGeneratorService } from "@formulary/services/_shared/index.js";
import type { FeatureRiskFindingRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteRaiseFeatureRiskFinding {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly featureRiskFinding: FeatureRiskFindingRepository
  ) {}

  async execute(input: RaiseFeatureRiskFindingInput): Promise<RaiseFeatureRiskFindingOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.gateId();

    // All field names extracted from OpenAPI spec: feature, mitigation, riskType, severity
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).reviewId) {
      throw new ValidationError("reviewId is required");
    }
    const reviewId = (input as any).reviewId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.featureRiskFinding.raiseFeatureRiskFinding({ reviewId: reviewId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${reviewId}`);
    }

    return entity as RaiseFeatureRiskFindingOutput;
  }
}
