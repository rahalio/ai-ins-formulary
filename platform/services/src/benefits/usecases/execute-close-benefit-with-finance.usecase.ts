/**
 * ExecuteCloseBenefitWithFinance - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: benefits
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteCloseBenefitWithFinance Use Case
 *
 * DDD: Application use case for get Close.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { CloseBenefitWithFinanceInput, CloseBenefitWithFinanceOutput } from "../dto/close.dto";
import type { ExecutionContextService, IdGeneratorService } from "@formulary/services/_shared/index.js";
import type { CloseRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteCloseBenefitWithFinance {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly close: CloseRepository
  ) {}

  async execute(input: CloseBenefitWithFinanceInput): Promise<CloseBenefitWithFinanceOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.benId();

    // All field names extracted from OpenAPI spec: confirmedBy, confirmedMagnitude, confirmedMonetary, financeOutcome, ledgerReference
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).claimId) {
      throw new ValidationError("claimId is required");
    }
    const claimId = (input as any).claimId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.close.closeBenefitWithFinance({ claimId: claimId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${claimId}`);
    }

    return entity as CloseBenefitWithFinanceOutput;
  }
}
