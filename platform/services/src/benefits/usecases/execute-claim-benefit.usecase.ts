/**
 * ExecuteClaimBenefit - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: benefits
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteClaimBenefit Use Case
 *
 * DDD: Application use case for get Claim.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ClaimBenefitInput, ClaimBenefitOutput } from "../dto/claim.dto";
import type { ExecutionContextService, IdGeneratorService } from "@formulary/services/_shared/index.js";
import type { ClaimRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteClaimBenefit {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly claim: ClaimRepository
  ) {}

  async execute(input: ClaimBenefitInput): Promise<ClaimBenefitOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.benId();

    // All field names extracted from OpenAPI spec: baselineDescription, baselineIsolated, benefitKind, candidateId, lineOfBusiness, magnitude, monetaryEquivalent, period
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).candidateId) {
      throw new ValidationError("candidateId is required");
    }
    const candidateId = (input as any).candidateId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.claim.claimBenefit({ candidateId: candidateId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${candidateId}`);
    }

    return entity as ClaimBenefitOutput;
  }
}
