/**
 * SetValuePosition handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: candidates
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { setValuePosition } from "@formulary/api-server/domains/candidates/handlers";
import type { CandidatesDDDDependencies } from "@formulary/api-server/domains/candidates";
import { createValuePosition as createValuePositionEntity } from "@formulary/factories/candidates";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@formulary/tests/shared";
import { CANDIDATES_USE_CASES } from "../__factories__/use-cases.js";

describe("SetValuePosition", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: CandidatesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<CandidatesDDDDependencies>(
      CANDIDATES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = createValuePositionEntity({ orgId });
    const input = {} as any;
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { candidateId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeSetValuePosition, "execute").mockResolvedValue(useCaseResult);

    await setValuePosition(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeSetValuePosition.execute).toHaveBeenCalledWith(expect.objectContaining({ candidateId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
